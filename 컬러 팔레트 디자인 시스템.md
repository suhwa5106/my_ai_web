# 🎨 컬러 팔레트 디자인 시스템

## 📝 프로젝트 정보
- **출처 웹사이트**: 카페 브랜드 봄 시즌 프로모션 (LOVE ME 캠페인)
- **분석 날짜**: 2025-12-12
- **적용 프로젝트**: 포트폴리오 웹사이트
- **디자인 테마**: 봄, 로맨틱, 소프트 파스텔

---

## 🎯 CSS 변수 정의

```css
:root {
  /* Primary Colors */
  --color-primary: #E84A8A;
  --color-primary-light: #F06A9E;
  --color-primary-dark: #D63A7A;

  /* Secondary Colors */
  --color-secondary: #FDF5F5;
  --color-accent: #8B6FC0;

  /* Background Colors */
  --color-bg-primary: #FFF8F8;
  --color-bg-secondary: #FDF5F5;
  --color-bg-tertiary: #FADADD;

  /* Text Colors */
  --color-text-primary: #2D2D2D;
  --color-text-secondary: #5A5A5A;
  --color-text-muted: #888888;

  /* Interactive Colors */
  --color-button-primary: #E84A8A;
  --color-button-hover: #D63A7A;
  --color-link: #E84A8A;
  --color-link-hover: #8B6FC0;

  /* Border Colors */
  --color-border-light: #F5C6D0;
  --color-border-medium: #E84A8A;

  /* Status Colors */
  --color-success: #4CAF50;
  --color-warning: #FFC107;
  --color-error: #E84A8A;
  --color-info: #8B6FC0;
}
```

---

## 🎨 컬러 사용 가이드

### Primary Color (`#E84A8A` 로즈 핑크)
| 사용 상황 | 예시 |
|----------|------|
| CTA 버튼 | 회원가입, 구매하기, 자세히 보기 |
| 강조 텍스트 | 중요 키워드, 가격, 할인율 |
| 아이콘 하이라이트 | 활성화된 메뉴, 선택된 항목 |
| 링크 텍스트 | 인라인 링크, 네비게이션 |

```css
.btn-primary {
  background-color: var(--color-button-primary);
  color: white;
  border: none;
  transition: background-color 0.3s ease;
}

.btn-primary:hover {
  background-color: var(--color-button-hover);
}
```

### Secondary Color (`#FDF5F5` 소프트 핑크)
| 사용 상황 | 예시 |
|----------|------|
| 페이지 배경 | 메인 섹션 배경 |
| 카드 배경 | 제품 카드, 정보 카드 |
| 입력 필드 배경 | 폼 요소 |

```css
.card {
  background-color: var(--color-bg-secondary);
  border-radius: 12px;
  padding: 24px;
}
```

### Accent Color (`#8B6FC0` 라일락)
| 사용 상황 | 예시 |
|----------|------|
| 보조 강조 | 배지, 태그, 라벨 |
| 호버 효과 | 링크 호버, 아이콘 호버 |
| 그라데이션 조합 | Primary와 함께 사용 |

```css
.tag {
  background-color: var(--color-accent);
  color: white;
  padding: 4px 12px;
  border-radius: 16px;
  font-size: 0.875rem;
}
```

### Text Colors 사용 기준
| 색상 | 용도 | 예시 |
|------|------|------|
| `--color-text-primary` | 제목, 본문 | h1~h3, 중요 문단 |
| `--color-text-secondary` | 부제목, 설명 | h4~h6, 캡션 |
| `--color-text-muted` | 보조 정보 | 날짜, 메타 정보, placeholder |

```css
h1, h2, h3 {
  color: var(--color-text-primary);
}

p {
  color: var(--color-text-secondary);
}

.meta, .caption {
  color: var(--color-text-muted);
}
```

---

## 📱 반응형 고려사항

### 다크모드 지원

```css
@media (prefers-color-scheme: dark) {
  :root {
    /* Primary Colors - 밝기 조정 */
    --color-primary: #F06A9E;
    --color-primary-light: #F8A0BE;
    --color-primary-dark: #E84A8A;

    /* Secondary Colors */
    --color-secondary: #2D2D2D;
    --color-accent: #A88FD4;

    /* Background Colors - 어두운 톤 */
    --color-bg-primary: #1A1A1A;
    --color-bg-secondary: #242424;
    --color-bg-tertiary: #2D2D2D;

    /* Text Colors - 밝은 톤 */
    --color-text-primary: #F5F5F5;
    --color-text-secondary: #CCCCCC;
    --color-text-muted: #888888;

    /* Interactive Colors */
    --color-button-primary: #F06A9E;
    --color-button-hover: #F8A0BE;
    --color-link: #F06A9E;
    --color-link-hover: #A88FD4;

    /* Border Colors */
    --color-border-light: #3D3D3D;
    --color-border-medium: #F06A9E;
  }
}
```

### 고대비 모드 (접근성)

```css
@media (prefers-contrast: high) {
  :root {
    --color-primary: #C41E5C;
    --color-text-primary: #000000;
    --color-text-secondary: #1A1A1A;
    --color-bg-primary: #FFFFFF;
    --color-border-medium: #000000;
  }
}
```

### 화면 크기별 고려사항

| 화면 크기 | 권장 사항 |
|----------|----------|
| **모바일** (< 768px) | 터치 영역 44px 이상, 버튼 색상 대비 강화 |
| **태블릿** (768px ~ 1024px) | 호버 효과 대신 탭 피드백 강조 |
| **데스크톱** (> 1024px) | 미묘한 호버 효과, 그라데이션 활용 |

```css
/* 모바일: 터치 친화적 버튼 */
@media (max-width: 768px) {
  .btn-primary {
    min-height: 48px;
    font-size: 1rem;
  }
}
```

---

## 🔧 유틸리티 클래스

```css
/* 배경색 유틸리티 */
.bg-primary { background-color: var(--color-primary); }
.bg-secondary { background-color: var(--color-secondary); }
.bg-accent { background-color: var(--color-accent); }

/* 텍스트색 유틸리티 */
.text-primary { color: var(--color-primary); }
.text-dark { color: var(--color-text-primary); }
.text-muted { color: var(--color-text-muted); }

/* 테두리 유틸리티 */
.border-primary { border-color: var(--color-border-medium); }
.border-light { border-color: var(--color-border-light); }
```

---

## 🎭 그라데이션 조합

```css
/* 메인 그라데이션 */
.gradient-primary {
  background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-accent) 100%);
}

/* 소프트 그라데이션 (배경용) */
.gradient-soft {
  background: linear-gradient(180deg, var(--color-bg-primary) 0%, var(--color-bg-tertiary) 100%);
}

/* 버튼 호버 그라데이션 */
.btn-gradient {
  background: linear-gradient(135deg, #E84A8A 0%, #8B6FC0 100%);
  transition: opacity 0.3s ease;
}

.btn-gradient:hover {
  opacity: 0.9;
}
```

---

## 📋 색상 접근성 체크리스트

| 조합 | 대비율 | WCAG AA | WCAG AAA |
|------|--------|---------|----------|
| Primary + White | 4.5:1 | ✅ 통과 | ✅ 통과 |
| Text Primary + BG Primary | 12.5:1 | ✅ 통과 | ✅ 통과 |
| Text Muted + BG Primary | 4.2:1 | ✅ 통과 | ❌ 미통과 |
| Accent + White | 4.8:1 | ✅ 통과 | ✅ 통과 |

> **참고**: 작은 텍스트(18px 미만)는 최소 4.5:1, 큰 텍스트(18px 이상)는 3:1 대비율 필요

---

## 📦 빠른 복사용 색상 코드

```
Primary:        #E84A8A
Primary Light:  #F06A9E
Primary Dark:   #D63A7A
Secondary:      #FDF5F5
Accent:         #8B6FC0
BG Primary:     #FFF8F8
BG Secondary:   #FDF5F5
Text Primary:   #2D2D2D
Text Secondary: #5A5A5A
Text Muted:     #888888
Border Light:   #F5C6D0
```
